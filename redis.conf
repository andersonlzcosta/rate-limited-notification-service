# Redis configuration for persistence
save 900 1
save 300 10
save 60 10000

# Enable AOF (Append Only File) for better persistence
appendonly yes
appendfsync everysec

# Directory for persistence files
dir /data

